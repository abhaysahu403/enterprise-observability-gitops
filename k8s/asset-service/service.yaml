---
apiVersion: v1
kind: Service
metadata:
  name: asset-service
  namespace: enterprise-observability
  labels:
    app: asset-service
spec:
  type: ClusterIP
  ports:
    - port: 8085
      targetPort: 8085
      protocol: TCP
  selector:
    app: asset-service