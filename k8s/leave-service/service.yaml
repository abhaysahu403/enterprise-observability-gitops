---
apiVersion: v1
kind: Service
metadata:
  name: leave-service
  namespace: enterprise-observability
  labels:
    app: leave-service
spec:
  type: ClusterIP
  ports:
    - port: 8083
      targetPort: 8083
      protocol: TCP
  selector:
    app: leave-service