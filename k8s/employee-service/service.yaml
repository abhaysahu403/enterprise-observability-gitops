---
apiVersion: v1
kind: Service
metadata:
  name: employee-service
  namespace: enterprise-observability
  labels:
    app: employee-service
spec:
  type: ClusterIP
  ports:
    - port: 8082
      targetPort: 8082
      protocol: TCP
      name: http
  selector:
    app: employee-service