---
apiVersion: v1
kind: Service
metadata:
  name: authentication-service
  namespace: enterprise-observability
  labels:
    app: authentication-service
spec:
  type: ClusterIP
  ports:
    - port: 8081
      targetPort: 8081
      protocol: TCP
  selector:
    app: authentication-service